-- This module is recommended for use only with the 'bundle' version
-- If you're using 'source' or 'rbxm' version, require classes directly

return {
    Theme = require("@self/Theme"),
    Icon = require("@self/Icon")
}